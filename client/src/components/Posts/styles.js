import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  mainContainer: {
    display: "flex",
    alignItems: "center",
    marginTop: 100,
  },
  smMargin: {
    margin: 10,
  },
  actionDiv: {
    textAlign: "center",
  },
}));
